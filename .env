# Environment variables for local development and Docker Compose.
# These values are non-sensitive and safe to commit.
# Sensitive settings (e.g., passwords) should be defined via:
#   - .env.local (gitignored)
#   - CI/CD environment variables
#   - Docker secrets or managed vaults
# You can override any of these settings in a .env.local file and Makefile will
# pick them up automatically.
#

# ------------------------------------------------------------------------------
# Local dev environment configuration
# ------------------------------------------------------------------------------

ASPNETCORE_ENVIRONMENT=Development

# ------------------------------------------------------------------------------
# Database configuration (non-secret)
# ------------------------------------------------------------------------------
# By default, use in-memory database for local dev.
#

# In-memory database for quick testing
DB_DRIVER=inmemory

# SQLite configuration for local dev
# DB_DRIVER=sqlite
DB_HOST=localhost
DB_NAME=diffs.db

# PostgreSQL configuration for app
# DB_DRIVER=postgres
POSTGRES_HOST=localhost
POSTGRES_DB=differgosum
POSTGRES_USER=differgosum
POSTGRES_PASSWORD=secret

# ------------------------------------------------------------------------------
# Secrets (should be overridden in .env.local)
# ------------------------------------------------------------------------------

YAAK_ENCRYPTION_KEY=
